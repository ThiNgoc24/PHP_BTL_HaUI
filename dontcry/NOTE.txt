				A CHẠY BẢN ĐÚP NÀY TRC ĐÃ NHỚ
- phần bảo trì có 3 layer
- các phần phía user ko có, đã thử nhưng ăn gậy warning vs error nên khum dám nghịch nữa
- cái conn_db.php: e viết toẹt ra cái connect vì lúc dùng hàm e bị lỏ ;))
			a xem lại nhớ. Có gì e dí vào đầu mỗi cái file php
- images thêm 1 ảnh banner: banner22.jpg 
- phần search result lỏ đoạn tìm kiếm, nó ra hheets mọe sp luôn, (nhóm mình có uc này khum) ko có để chiều e tra lại gpt
- ở Home: 
	+ chỗ Nav có mục SÁCH: ấn vô nó ra dsach sp lấy từ setail/list_pd.php
	+ chỗ danh mục sách: ấn vô hiện ds sp theo danh mục-> ấn vào xem chi tiết -> hiện ctsp
		(ấn quay lại là về home)

- ở bảo trì: mục index.php có nút quay lại a chỉ cần dí thêm link page con home bảo trì là uke
- ở DB: 
	+ thêm ràng buộc khóa ngoại:
		ALTER TABLE `products`
  		ADD CONSTRAINT `fk_author_id` FOREIGN KEY (`author_id`) REFERENCES `authors` (`id`),
  		ADD CONSTRAINT `fk_cat_id` FOREIGN KEY (`cat_id`) REFERENCES `categories` (`id`);
	+ tên db test: starbook_db

		VÀ E CHƯA BIẾT ĐẨY GIT VÔ NHÁNH ;)))))) A CHỈ E VS hâu hâu